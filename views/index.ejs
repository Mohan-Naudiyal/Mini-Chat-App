<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat App</title>
</head>
<body>
     <h1>Mini Chat app</h1>
     <form action="/chats/new" method="Get"><button>Add New Chat</button></form><br><br>

     <%for(let chat of chats) {%>
      <div style="background-color: aqua; border-radius: 15px; width: 300px; padding: 10px;">
        <h3>From : <%= chat.from %></h3>
        <div style="background-color: blue; border-radius: 15px; padding: 5px; "><p >Message : <%= chat.msg %></p>
         <form action="/chats/<%= chat._id%>/edit" method="get">
           <button style="margin-left: 15px; background-color: chartreuse;">edit</button>
        </form>
        <br>
        <form action="/chats/<%= chat._id%>?_method=delete" method="post">
          <button style="margin-left: 15px; background-color: chartreuse;">delete</button>
       </form>
        </div>
        <h3>From : <%= chat.to %></h3>
        <p><%= chat.Create_at.toString().split(" ")[4] %></p>
        <p><%= chat.Create_at.toString().split(" ").slice(0,4).join("-") %></p>

      </div>
       <hr>

     <%} %>
</body>
</html>